import { NotAllowed } from "@/components";

type TOwnProps = {};
const NotAllowPage = (props: TOwnProps) => {
  const {} = props;
  return (
    <div className="not-allowed">
      <NotAllowed />
    </div>
  );
};
export default NotAllowPage;
